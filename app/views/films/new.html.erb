<h1>New Film</h1>
<%= form_for :film, url: films_path do |f| %>

	<% if @film.errors.any? %>
	  <div id="error_explanation">
	    <h2><%= pluralize(@film.errors.count, "error") %> prohibited
	      this film from being saved:</h2>
	    <ul>
	    <% @film.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', films_path %>